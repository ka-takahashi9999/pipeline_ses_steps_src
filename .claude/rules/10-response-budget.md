# Response Budget

応答速度と出力量の制御ルール。

- 結論を先に3〜5行で出す
- ログ全文・JSONL全文・コード全文を貼らない
- 代表3件で報告し、全件列挙しない
- 実装進捗は短文（1〜2行）にする
- 「問題なし」でも長い説明をしない
- 出力が長くなりそうなら自分で要約して返す
- 1回の返答で大量のJSONやログ断片を出さない
- レビュー時はfindingsを先に簡潔に出してから詳細を続ける
- 分割が必要なら「まず代表3件 → 次に全体集計」の順で出す
